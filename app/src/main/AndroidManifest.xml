<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Android 17+ (API 37): required to reach servers on the local network —
         both SSDP/DLNA discovery and direct connections to LAN hosts
         (e.g. 192.168.x.x). Declared as a runtime permission and requested
         from MainActivity on cold start, mirroring POST_NOTIFICATIONS.
         See https://developer.android.com/privacy-and-security/local-network-permission -->
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Required for ExoPlayer.setWakeMode to actually acquire the wakelock
         (without it setWakeMode silently no-ops). WAKE_MODE_NETWORK additionally
         holds a Wi-Fi multicast lock so CPU/Wi-Fi stay awake during backgrounded
         HTTP streaming playback; WAKE_MODE_LOCAL is intended for local files. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Audio effects (Equalizer/BassBoost/Virtualizer/Reverb/LoudnessEnhancer) and Visualizer -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- TV Watch Next publishing (only used on leanback devices; ignored elsewhere) -->
    <uses-permission android:name="com.android.providers.tv.permission.WRITE_EPG_INPUT" />

    <!-- Floating overlay player (TYPE_APPLICATION_OVERLAY) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- In-app self-update: lets the system package installer prompt to
         install an APK we download to the cache dir and share via FileProvider. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:name=".JellyPlayApplication"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.JellyPlay"
        android:usesCleartextTraffic="true"
        tools:targetApi="37">

        <!-- BIN-9 (audit): StartupBenchmark measures the release build with
             CompilationMode.None; on API 29+ macrobenchmark/systrace visibility
             of a non-debuggable app requires this element, otherwise release
             timings silently degrade. Harmless on debug builds (debuggable
             implies profileability). -->
        <profileable android:shell="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.JellyPlay.Starting"
            android:windowSoftInputMode="adjustResize"
            android:resizeableActivity="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|uiMode|keyboard|keyboardHidden|navigation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="jellyplay" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="raulshma.github.io"
                    android:pathPrefix="/jellyplay" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!-- Fullscreen video host. Shares the default taskAffinity with
             MainActivity so entering PiP here reveals the browse UI behind the
             floating window (back-stack choreography). adjustResize so IME insets
             reach PlayerModalBottomSheet inputs (subtitle language, lyrics search). -->
        <activity
            android:name=".PlayerActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|uiMode|keyboard|keyboardHidden|navigation" />

        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <meta-data
            android:name="com.android.automotive"
            android:resource="@xml/automotive_app_desc" />

        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.raulshma.jellyplay.cast.JellyPlayCastOptionsProvider" />

        <service
            android:name=".floating.FloatingPlayerService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <receiver
            android:name=".widget.NowPlayingWidget"
            android:exported="true"
            android:label="@string/widget_now_playing_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/now_playing_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.ContinueWatchingWidget"
            android:exported="true"
            android:label="@string/widget_continue_watching_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.raulshma.jellyplay.widget.ACTION_REFRESH_CONTINUE_WATCHING" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/continue_watching_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.LibraryRecommendationsWidget"
            android:exported="true"
            android:label="@string/widget_library_recommendations_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.raulshma.jellyplay.widget.ACTION_REFRESH_LIBRARY" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/library_recommendations_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.SeerrRecommendationsWidget"
            android:exported="true"
            android:label="@string/widget_seerr_recommendations_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.raulshma.jellyplay.widget.ACTION_REFRESH_SEERR" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/seerr_recommendations_widget_info" />
        </receiver>

        <service
            android:name=".widget.ContinueWatchingWidgetService"
            android:exported="false"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <service
            android:name=".widget.LibraryRecommendationsWidgetService"
            android:exported="false"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <service
            android:name=".widget.SeerrRecommendationsWidgetService"
            android:exported="false"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <activity
            android:name=".widget.config.LibraryWidgetConfigActivity"
            android:exported="true"
            android:theme="@style/Theme.JellyPlay.Starting"
            android:label="@string/widget_library_recommendations_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <activity
            android:name=".widget.config.SeerrWidgetConfigActivity"
            android:exported="true"
            android:theme="@style/Theme.JellyPlay.Starting"
            android:label="@string/widget_seerr_recommendations_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <activity
            android:name=".widget.config.ContinueWatchingWidgetConfigActivity"
            android:exported="true"
            android:theme="@style/Theme.JellyPlay.Starting"
            android:label="@string/widget_continue_watching_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <activity
            android:name=".widget.config.NowPlayingWidgetConfigActivity"
            android:exported="true"
            android:theme="@style/Theme.JellyPlay.Starting"
            android:label="@string/widget_now_playing_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>
        <receiver android:name="androidx.mediarouter.media.MediaTransferReceiver" />

        <receiver
            android:name="com.raulshma.jellyplay.core.notification.receiver.NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.raulshma.jellyplay.notification.OPEN_DETAIL" />
                <action android:name="com.raulshma.jellyplay.notification.MARK_SEEN" />
                <action android:name="com.raulshma.jellyplay.notification.MARK_ALL_SEEN" />
            </intent-filter>
        </receiver>

        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <service
            android:name=".tile.JellyPlayTileService"
            android:exported="true"
            android:icon="@drawable/ic_launcher_foreground"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>

</manifest>
